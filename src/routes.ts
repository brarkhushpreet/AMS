export const authRoutes = ["/auth/login", "/auth/signup"];
export const apiAuthPrefix = "/api/auth";
export const publicRoutes = ["/", "/api/attendance/signing-key"];
export const publicRoutePrefixes = [
  "/verify/attendance/",
  "/api/attendance/reports/",
];
